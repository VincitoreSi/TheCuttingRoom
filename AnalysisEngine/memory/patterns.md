# Learned patterns (auto-appended, deduped)

_One distilled lesson per line, appended by the judge at the end of each run (step 9) and fed
back into the system prompt on the next one._

This ships **empty on purpose.** Every entry is distilled from a specific real clip you
analyzed, so a populated copy is your own working data — quoting a creator's wardrobe, palette
or sound back at you. Yours will fill in after the first `uv run cli.py run <platform>`.
