# Trending terms memory (auto-appended, deduped)

No trending-term insights recorded yet — this file is populated from the shared
`trending-terms` insight tag over time (`POST /api/insights`, read back via
`GET /api/insights` at the top of each run).
